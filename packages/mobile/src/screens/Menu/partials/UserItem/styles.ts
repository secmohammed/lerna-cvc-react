import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  conatainter: {
    height: 115,
    width: '100%',
    marginBottom: 15,
    borderRadius: 10,
    elevation: 8
  }
});
